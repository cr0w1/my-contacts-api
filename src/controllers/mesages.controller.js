const { getConnection } = require('../database');
const { v4 } = require('uuid');


// Get
const getMesage = (req, res) => {

}

// Get
const getMesage = (req, res) => {

}

// Save
const getMesage = (user_id, msg) => {

    const newMesage = {
        id: v4(),
        user_id: user_id,
    }
}

// Get
const getMesage = (req, res) => {

}

module.exports = {

}